name: Deploy

on:
  push:
  workflow_dispatch:

jobs:
  api:
    name: Build API
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'zulu'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build
        run: ./gradlew --no-daemon :api:bootJar

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: api-${{ github.sha }}
          path: desktop-v2/api.jar
          retention-days: 1

  desktop:
    needs: api
    name: Build Desktop App
    runs-on: ${{ matrix.os }}

    defaults:
      run:
        working-directory: desktop-v2

    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest ]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Download API
        uses: actions/download-artifact@v3
        with:
          name: api-${{ github.sha }}
          path: desktop-v2

      - name: Install Dependencies
        run: npm i

      - name: Install Linux Build Dependencies
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: sudo apt-get install -y rpm

      - name: Build Electron
        run: npm run electron:build

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: nebulosa-${{ matrix.os }}-${{ github.sha }}
          path: |
            desktop-v2/release/nebulosa*.AppImage
            desktop-v2/release/nebulosa*.deb
            desktop-v2/release/nebulosa*.rpm
            desktop-v2/release/nebulosa*.exe
            desktop-v2/release/nebulosa*.dmg
          retention-days: 3
